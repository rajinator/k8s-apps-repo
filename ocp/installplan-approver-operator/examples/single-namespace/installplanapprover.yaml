---
apiVersion: operators.bapu.cloud/v1alpha1
kind: InstallPlanApprover
metadata:
  name: single-namespace-approver
  namespace: operators
spec:
  # Enable automatic approval
  autoApprove: true
  
  # Target only one specific namespace
  targetNamespaces:
    - cert-manager
  
  # Optional: Approve only specific operators
  # operatorNames:
  #   - cert-manager

