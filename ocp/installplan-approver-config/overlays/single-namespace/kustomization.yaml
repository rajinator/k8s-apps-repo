---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Single-namespace InstallPlanApprover configuration
# For testing or isolated operator approval

namespace: iplan-approver-system

resources:
  - installplanapprover.yaml

labels:
  - pairs:
      app.kubernetes.io/name: installplan-approver-config
      app.kubernetes.io/component: config
      config-type: single-namespace

