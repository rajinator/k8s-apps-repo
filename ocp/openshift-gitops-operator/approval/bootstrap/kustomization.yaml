---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# One-time bootstrap Job for initial InstallPlan approval
# Use this during manual cluster bootstrap

resources:
  - installplan-approver-rbac.yaml
  - installplan-approver-job.yaml

labels:
  - pairs:
      app.kubernetes.io/name: installplan-approver
      app.kubernetes.io/component: bootstrap

